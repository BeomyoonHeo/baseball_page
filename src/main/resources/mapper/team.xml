<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.baseball.domain.team.TeamDao">


	<select id="findAll"
		resultType="com.example.baseball.web.dto.response.team.TeamListDto">
		SELECT t.id, (SELECT s.NAME FROM stadium s WHERE s.id =
		t.stadiumid)
		stadiumname, t.name, DATE_FORMAT(t.createDate,
		'%Y년-%m월-%d일')createDate,
		ROW_NUMBER() OVER (ORDER BY t.name ASC) AS
		ROW
		FROM team t;
	</select>

	<select id="findTeamName" resultType="string">
		SELECT distinct name FROM team
	</select>

	<select id="findByName"
		resultType="com.example.baseball.domain.team.Team">
		SELECT * FROM stadium WHERE name = #{name}
	</select>

	<insert id="insert">
		INSERT INTO team(name, stadiumId, CreateDate)
		VALUE(#{name},#{stadiumId} , NOW());
	</insert>

	<delete id="deleteById">
		DELETE FROM team WHERE id = #{id}
	</delete>

	<update id="update">
		UPDATE team SET name = #{name} WHERE id =#{id}
	</update>


</mapper>