<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.baseball.domain.player.PlayerDao">


	<select id="findAll"
		resultType="com.example.baseball.web.dto.response.player.PlayerListDto">
		SELECT id, (SELECT NAME FROM team t WHERE t.id = p.teamId)
		teamname,
		p.name,p.position ,DATE_FORMAT(p.createDate,
		'%Y년-%m월-%d일')createDate,
		ROW_NUMBER() OVER (ORDER BY p.name ASC) AS ROW FROM player p
		WHERE p.expulsion is null
		<if test="id != null">
		AND teamid = #{id}
		</if>
	</select>
	
	<select id="findGroupForPosition"
		resultType="map">
		SELECT
		<foreach collection="list" item="item">
		MAX(if(t.name = #{item}, p.name, null)) #{item}, 
		</foreach>
		POSITION
		FROM player p INNER JOIN team t
		ON p.teamid = t.id
		GROUP BY p.position
	</select>

	<select id="findByName"
		resultType="com.example.baseball.domain.player.Player">
		SELECT * FROM player WHERE name = #{name}
	</select>

	<insert id="insert">
		INSERT INTO player(name, teamId, position,
		createDate)
		VALUE(#{name}, #{teamId}, #{position}, NOW());
	</insert>

	<delete id="deleteById">
		DELETE FROM player WHERE id = #{id}
	</delete>

	<update id="update">
		UPDATE player SET expulsion = 1 WHERE id =#{id}
	</update>


</mapper>